query DeliveryOptionsInput($namespace: String!, $key: String!) {
  cart {
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: $namespace, key: $key) {
      jsonValue
    }
  }
}